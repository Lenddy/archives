//! shift and  unshift

const Array = [];

Array.shift(); //The shift() method removes the first element in the array and "shifts" all other elements to a lower index.   : it can returns the value that was "shifted out(deleted)

Array.unshift(32); // The unshift() method adds a new element to the beginning of the array, and "unshifts" older elements: it can returns the new array length:
